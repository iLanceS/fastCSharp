using System;

namespace fastCSharp.demo.serializePerformance
{
    /// <summary>
    /// 代码生成测试数据（为了不重复定义同样的测试数据，直接继承自Emit序列化的测试数据）
    /// </summary>
    [fastCSharp.code.cSharp.dataSerialize(IsMemberMap = false)]
    partial class codeFiledData : filedData
    {
    }
}
